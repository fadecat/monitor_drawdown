targets:
  - name: "红利低波100ETF(博时)"
    code: "159307"
    type: "etf"
    threshold: 0.05
    lookback_days: 120

  - name: "价值100ETF"
    code: "512040"
    type: "etf"
    threshold: 0.05
    lookback_days: 120

  # 你可以继续添加更多监控标的，例如：
  # 测试 webhook 时可临时把 threshold 改成 0，测完改回正式值
  # - name: "示例ETF"
  #   code: "515100"
  #   type: "etf"
  #   threshold: 0.10
  #   lookback_days: 180

calendar_monitors:
  - name: "下修股东会提醒"
    qtype: "CNV"
    window: "next_month"
    webhook_env: "CALENDAR_WEBHOOK_URL"
    title_keywords:
      - "下修股东会"
    lookahead_days: 45
